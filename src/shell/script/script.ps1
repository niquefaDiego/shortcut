# ---------- shortcut start ----------
# # This code was auto-generated by `$ shortcut setup`.
# See `$ shortcut -- help` for more info.
function s {
    param (
        [Parameter(Mandatory = $false)]
        [switch]$l,
        [Parameter(Mandatory = $false)]
        [string]$a,
        [Parameter(Mandatory = $false)]
        [string]$r,
        [Parameter(Mandatory = $false)]
        [switch]$b,
        [Parameter(Position = 0, Mandatory = $false)]
        [string]$unnamedArg
    )
    if ($PSBoundParameters.ContainsKey('a')) {
        shortcut add "$a" "$unnamedArg"
    } elseif ($PSBoundParameters.ContainsKey('r')) {
        shortcut remove "$r"
    } elseif ($l) {
        shortcut list
    } elseif ($b) {
        Pop-Location
    } else {
        $dir = shortcut get "$unnamedArg"
        Push-Location "$dir"
    }
}
# ---------- shortcut end ----------
